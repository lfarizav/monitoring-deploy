apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Alert
metadata:
  name: monitoring-deploy
  namespace: monitoring
spec:
  eventSeverity: info
  eventSources:
  - kind: HelmRelease
    name: prom
  providerRef:
    name: monitoring-deploy
